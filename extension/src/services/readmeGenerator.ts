import type { SolutionMetadata } from "@codevault/shared";
export const createSolutionReadme = (m: SolutionMetadata) => `# ${m.problemId}. ${m.title}\n\n| Detail | Value |\n| --- | --- |\n| Difficulty | ${m.difficulty} |\n| Language | ${m.language} |\n| Runtime | ${m.runtime ?? "—"} |\n| Memory | ${m.memory ?? "—"} |\n\n## Topics\n\n${m.topics.length ? m.topics.map(topic => `\`${topic}\``).join(" ") : "Not available"}\n\n## Solution\n\nSee the accompanying solution file for the submitted implementation.\n\n## Links\n\n- [LeetCode problem](${m.leetCodeUrl})\n- [GitHub source](${m.githubUrl})\n`;
